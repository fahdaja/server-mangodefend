export enum ScanType {
  UPLOAD_FILE = 'upload_file',
  FULL_SCAN = 'full_scan',
}

export enum ScanStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
